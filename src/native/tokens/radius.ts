/**
 * ADS Native radius tokens (points).
 */

export type ADSRadiusTokens = {
    none: number;
    sm: number;
    md: number;
    lg: number;
    xl: number;
    full: number;
};

export const adsRadius: ADSRadiusTokens = {
    none: 0,
    sm: 6,
    md: 8,
    lg: 12,
    xl: 16,
    full: 9999
};
